{
  "name": "@onyx/mcp",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@onyx/sdk": "workspace:*",
    "@onyx/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "typescript": "^5.7.0"
  }
}
